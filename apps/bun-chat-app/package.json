{
  "name": "@encatch/bun-chat-app",
  "version": "0.1.0",
  "private": true,
  "description": "Bun-based shell for the same @encatch/core + @encatch/ui chat app (browser client + Bun.serve backend)",
  "scripts": {
    "dev": "bun --hot run src/server/index.ts",
    "build": "bun build ./src/server/index.ts --target=bun --outdir=dist",
    "start": "NODE_ENV=production bun run dist/index.js",
    "dev:desktop": "bun run src/desktop/main.ts",
    "build:desktop": "bun build --compile --minify --sourcemap ./src/desktop/main.ts ./src/desktop/server-worker.ts --outfile dist/encatch-desktop",
    "lint": "echo \"TODO: lint\"",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@encatch/core": "workspace:*",
    "@encatch/ui": "workspace:*",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "webview-bun": "^2.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "rimraf": "^6.1.3",
    "typescript": "^7.0.2"
  }
}
